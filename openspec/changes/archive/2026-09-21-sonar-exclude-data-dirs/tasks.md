## 1. Configuración

- [x] 1.1 Añadir `data/**`, `logs/**` y `.scannerwork/**` a `sonar.exclusions` en `sonar-project.properties`.
- [x] 1.2 Añadir `.scannerwork/` a `.gitignore`.

## 2. Verificación

- [x] 2.1 Confirmar que `.scannerwork/` generado localmente no queda trackeado. (`git status --short` ya no lo lista tras añadirlo a `.gitignore`.)
