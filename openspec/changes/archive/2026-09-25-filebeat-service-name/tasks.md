## 1. Filebeat

- [x] 1.1 Añadir processor global `add_fields` con `target: service` y `name: mydiary` en `docker/filebeat/filebeat.yml`
- [x] 1.2 Validar la configuración con `filebeat test config` (imagen de Filebeat)

## 2. Documentación

- [x] 2.1 Documentar `service.name` en `Especificaciones.md` (sección de logs/Kibana)
- [x] 2.2 Añadir entrada en `CHANGELOG.md` (`## [Sin publicar]`) con nota de despliegue
