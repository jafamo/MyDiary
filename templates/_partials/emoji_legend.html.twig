{% if legend is not empty %}
    <p class="emoji-legend">
        {%- for entry in legend -%}
            {{ loop.first ? '' : ' · ' }}{{ entry.emoji }} {{ entry.meaning }}
        {%- endfor -%}
    </p>
{% endif %}
